package com.dreamit.proxystat.model.factory;

import java.util.Date;

import com.dreamit.proxystat.model.entities.Season;

/**
 * Season factory for controllers
 * @author acraske
 *
 */
public interface ISeasonFactory {

	/**
	 * factory method, don't persist the object
	 * @param name
	 * @param description
	 * @return a new instance of a season
	 */
	Season createSeason(String name, Date startDate, Date endDate);
	
}
