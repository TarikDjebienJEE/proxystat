package com.dreamit.proxystat.model.factory;

import com.dreamit.proxystat.model.entities.Partner;

/**
 * Partner factory for controllers
 * @author tarik djebien
 *
 */
public interface IPartnerFactory {
	
	/**
	 * Partner method, don't persist the object
	 * @return a new instance of a Partner
	 */
	Partner createPartner(String name, String description);

}
